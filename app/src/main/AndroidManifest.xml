<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.keepfitting.jit.keepfitting">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.zpermission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tubiao"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity" >

        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".LoginActivity">

        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="@string/title_activity_register" />
        <activity android:name=".KonwMeActivity" />
        <activity android:name=".ModifyUserActivity" />
        <activity android:name=".LoginByPwdActivity" />
        <activity android:name=".GoalDetailsActivity" />
        <activity android:name=".GoalActivity" />
        <activity android:name=".GoalAddActivity" />
        <activity android:name=".ShowFoodActivity" />
        <activity android:name=".ShowSportActivity" />
        <activity android:name=".AddFoodConfirmActivity"
            android:theme="@style/ActivityDialogTheme"/>
        <activity android:name=".AddSportConfirmActivity"
            android:theme="@style/ActivityDialogTheme"/>
        <activity android:name=".FoodConditionActivity" />
        <activity android:name=".SportConditionActivity"></activity>
    </application>

</manifest>